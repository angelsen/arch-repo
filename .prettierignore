# Generated by `ap-dev update` via json.dump(indent=2), which expands every
# array across multiple lines. Prettier collapses the short ones back onto a
# single line, so formatting and publishing would rewrite this file in opposite
# directions on every cycle. The generator owns this file.
packages.json
packages.json.bak
